// js/page-index.js

document.addEventListener("DOMContentLoaded", () => {
  console.log("หน้า Home พร้อมทำงาน!");
  
  // 💡 ไอเดียสำหรับอนาคต:
  // สามารถเขียนสคริปต์ทำ Scroll Animation (เลื่อนลงมาแล้วข้อความค่อยๆ โผล่) 
  // หรือ Typing Effect (ตัวหนังสือพิมพ์เอง) ได้ที่ไฟล์นี้ โดยไม่กระทบหน้าอื่น
});